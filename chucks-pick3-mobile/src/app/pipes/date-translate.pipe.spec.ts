import { DateTranslatePipe } from './date-translate.pipe';

describe('DateTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
